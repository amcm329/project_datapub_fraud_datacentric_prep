# project_datapub_fraud_datacentric_prep
Data-centric preprocessing for an e-commerce fraud dataset (The Data Pub, Mexico). Converts 24 item slots into per-product count features, adds total purchase, merges similar product columns via TF-IDF cosine similarity, and drops redundant/weak features using correlation filters.
